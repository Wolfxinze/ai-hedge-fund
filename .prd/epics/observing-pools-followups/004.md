---
name: README SERVER_BIND_HOST hardening + doc-guard
status: open
created: 2026-07-04
updated: 2026-07-04
github: null
depends_on: []
parallel: true
conflicts_with: []
lane: C
---

# Task: README SERVER_BIND_HOST hardening + doc-guard

## Description

The §19 startup gate keys on `SERVER_BIND_HOST`, but the READMEs document bare
`uvicorn main:app --reload` with no such env var — a copy-paste non-loopback deploy could
bypass the gate. Fix the docs and pin them with a doc-guard test. Docs + guard only — no
bind-logic change (Woo, 2026-07-04).

## Acceptance Criteria

- [ ] `app/backend/README.md` and `app/README.md` uvicorn examples set `SERVER_BIND_HOST=127.0.0.1` and note that non-loopback deploys must set it (the gate refuses otherwise).
- [ ] A doc-guard test under `tests/docs/` asserts both READMEs mention `SERVER_BIND_HOST`.
- [ ] `grep -q SERVER_BIND_HOST app/backend/README.md && grep -q SERVER_BIND_HOST app/README.md` exits 0.
- [ ] `.venv/bin/python -m pytest tests/docs -q` passes.

## Technical Details

- Files: `app/backend/README.md:64`, `app/README.md:143` (the `poetry run uvicorn main:app --reload` lines).
- New test: `tests/docs/test_readme_bind_host.py` (sibling of `test_prd_reconcile.py`), reading
  both READMEs and asserting the `SERVER_BIND_HOST` mention — mirror the existing doc-guard style.
- Do NOT change `src/compliance.py` or `app/backend/main.py` (behavior is correct; this is docs).

## Dependencies

- depends_on: none.
- conflicts_with: none (READMEs + new test file). Wave 1, parallel.

## Effort Estimate

- Size: S
- Hours: 0.5

## Definition of Done

- [ ] READMEs updated + doc-guard test written and passing
- [ ] No production `.py` changed (docs + tests/docs only)
