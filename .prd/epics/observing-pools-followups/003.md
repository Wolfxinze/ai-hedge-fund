---
name: signoff loopback/unset asserts no side effects
status: open
created: 2026-07-04
updated: 2026-07-04
github: null
depends_on: []
parallel: true
conflicts_with: []
lane: B
---

# Task: signoff loopback/unset asserts no side effects

## Description

Strengthen `test_loopback_and_unset_never_raise` so it verifies the "byte-for-byte unchanged"
no-op the code + comment claim, not merely that the call does not raise.

## Acceptance Criteria

- [ ] For each loopback/unset host, the test asserts the gate creates NO file at the signoff path and mutates nothing (e.g. the injected env mapping is unchanged; no `evals_runs/` artifact appears).
- [ ] The assertion is mutation-meaningful — it would fail if the no-op path gained a side effect (a stat/write/read-through).
- [ ] `.venv/bin/python -m pytest tests/observing_pools/test_signoff_gate.py -k "never_raise or no_op" -q` passes.

## Technical Details

- File: `tests/observing_pools/test_signoff_gate.py` — `test_loopback_and_unset_never_raise` (~L27).
- Point `signoff_path` at a `tmp_path` file, call the gate, then assert `not path.exists()` and
  that the passed env dict is untouched. Keep the existing parametrization.

## Dependencies

- depends_on: none.
- conflicts_with: none (own file). Wave 1, parallel.

## Effort Estimate

- Size: XS
- Hours: 0.5

## Definition of Done

- [ ] Assertion added and passing
- [ ] No production `.py` changed (tests-only)
