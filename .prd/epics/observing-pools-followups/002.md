---
name: GET /monitors/{id} asserts no DB commit
status: open
created: 2026-07-04
updated: 2026-07-04
github: null
depends_on: []
parallel: false
conflicts_with: [001]
lane: A
---

# Task: GET /monitors/{id} asserts no DB commit

# Description

Strengthen `test_get_single_monitor_is_read_only` so it verifies the read-only contract its
own docstring already claims ("no commit"). Today it only asserts the scheduler job is
untouched — nothing proves the request issues no DB write.

## Acceptance Criteria

- [ ] The read-only test asserts GET `/monitors/{id}` performs no DB commit (e.g. spy/patch `Session.commit` and assert not called, or assert row state / `updated_at` is unchanged across the GET).
- [ ] The added assertion is mutation-meaningful — it would fail if the handler committed.
- [ ] `.venv/bin/python -m pytest tests/observing_pools/test_api_monitors.py -k "read_only" -q` passes.

## Technical Details

- File: `tests/observing_pools/test_api_monitors.py` — `test_get_single_monitor_is_read_only` (~L978).
- Simplest: monkeypatch/wrap the session's `commit` to count calls for the GET, or capture a
  pre/post snapshot of the row and assert equality. Keep it within the existing fixture.

## Dependencies

- depends_on: none.
- conflicts_with: 001 (same file) — same lane, sequential.

## Effort Estimate

- Size: XS
- Hours: 0.5

## Definition of Done

- [ ] Assertion added and passing
- [ ] No production `.py` changed (tests-only)
