---
name: DELETE preserves referencing opportunity_reports
status: open
created: 2026-07-04
updated: 2026-07-04
github: null
depends_on: []
parallel: false
conflicts_with: [002]
lane: A
---

# Task: DELETE preserves referencing opportunity_reports

## Description

Add a test proving the §14 soft-delete's *reason for being*: a monitor is soft-deleted
(`enabled=False`, row kept) specifically so referencing `opportunity_reports` survive. The
row-survives property is tested; the report-survives corollary is not.

## Acceptance Criteria

- [ ] A test seeds a monitor, drives it so an `opportunity_reports` row referencing it is persisted, DELETEs the monitor (204), then re-reads the report and asserts it is still returned.
- [ ] The test reads the report back through a fresh session/request (not the delete response), so it proves durable referential survival.
- [ ] `.venv/bin/python -m pytest tests/observing_pools/test_api_monitors.py -k "delete and report" -q` passes.

## Technical Details

- File: `tests/observing_pools/test_api_monitors.py` (DELETE section, ~L873+).
- Reuse the existing `env` fixture + analyzing-flow override and the report-persistence path
  the surrounding refresh/report tests already use (see the `/opportunity-reports` tests near
  L620 and the report-seeding helpers). Prefer driving a real refresh so the FK is genuine.
- Assert via `GET /opportunity-reports` (or the report table read) that the report row remains
  after the monitor DELETE.

## Dependencies

- depends_on: none.
- conflicts_with: 002 (same file `test_api_monitors.py`) — cannot run in parallel; same lane.

## Effort Estimate

- Size: S
- Hours: 1

## Definition of Done

- [ ] Test written and passing
- [ ] Test is mutation-meaningful (would fail if DELETE hard-dropped the row / cascaded reports)
- [ ] No production `.py` changed (tests-only)
