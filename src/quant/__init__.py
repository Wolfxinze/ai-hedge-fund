"""Pure quant math shared by scoring paths. Stdlib only — no I/O, no agent imports
(I1: this package must stay importable from the scoring path without reopening the
trade path; see ``src/quant/volatility.py``)."""
